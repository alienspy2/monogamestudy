project1 - demo project for engine development 

note
  * unity 엔진과 비슷한 interface 를 지향. unity 엔진으로부터 쉽게 porting 할 수 있도록 한다
  * 범용엔진을 지향하지 않는다.
    - 지향점 1 코딩 편의성
    - 지향점 2 실행속도
    - 지향점 3 렌더링 퀄리티
  * monogame 은 왼손 좌표계이고, unity 는 오른손 좌표계 이다. 
    따라서 유니티와 좌표계를 똑같이 맞출 수가 없다.
    - MGA 에서는 -Z 가 forward 이고, unity 에서는 +Z 가 forward 이다
    - 그 외에는 동일하다.
  * MGCB 는 shader 외에는 쓰지 말자
    - 쓰기도 번거롭고 지원하지 않는 asset 은 어차피 빌드해주지 않는다. 예) 3D mesh
    - asset build 시 옵션이 부족하다. 예) dds 압축시 quality 설정이 불가
  * 2D만 쓰려다가 3D 로 전환중. 따라서 여러가지를 고쳐야 한다.
  * blender 에서 glb 로 저장하는 것을 표준으로 삼는다. export 시 up = +Y 옵션을 지정한다
    - blender 의 +Z 가 MGA 의 +Y = forward
    - blender 의 -Y 가 MGA 의 -Z = forward 

BUG 
  * aa spirte 삭제시 제대로 비워지지 않는것 같다 -> aa sprite 는 제거하자
  * auto atlas sprite texture 로 load 할 때 sprite 를 매번 load 한다. 옳지 않다 -> aa sprite 는 제거하자
  * dynamic texture manager 의 repack 이 제대로 동작하지 않음
  * OnDisable 이 제대로 호출되지 않는다.
    - game object 가 비활성화 될 때
    - component.enabled 가 false 로 바뀔 때


TODO  

2D
  * AA 별로다. static texture atlas packing 만들자.
  * 2D sprite animation
  * sprite renderer 에 slice 기능을 넣거나 sliced sprite renderer 만들기
  * 임시로 만든 UI 정리
  * AA sprite 제거 <- text 용은 따로 만든다 

3D 
  * sharp gltf 붙이기 
    -> 어디까지 할까? 
      scene 전체를 불러올수도 있고, 
      unity 처럼 mesh, animation 등을 따로 불러올 수도 있다
  * navmesh (recast?) 붙이기
  * 물리엔진 (bepuphysics?) 붙이기 
    * 일단 대충 붙여보는 중
    * mesh collider 추가
    * 만들었다 지웠다 하는데, 뭔가 계속 쌓이나보다. 예제를 참고해서 제대로 붙이자
  * PBR shader ?
  * matcap shader ?
  * lighting 
  
고민중
  * web UI 를 추가할까?

기능 개선
  * 한글이 안되더라도, TTF 말고 디버깅용 fixed font 있어야겠다
  * text fitting 개선 - anchordRect 와 pivot 이 아직 제대로 안됨
  * input manager 에 was pressed 외에 repeating 추가
  * ui raycast 가 카메라 별로 따로 돈다. 모든 카메라에 대해 1개만 pick 되어야한다.
  * 키입력등이 focus 된 개체에만 적용되어야 한다.
  * uitransform 의 sizefitter 만들기 : children 의 크기에 맞게 자동으로 크기를 조절해주는 컴포넌트
  * text input 에 SDL 의 ime 붙이기
  * window manager 필요. 마지막으로 선택한 윈도우가 위로 올라와야 한다
  * window docking, snapping
  * alpha bleding 된 애들은 back to front 로 sort 하여야 한다
  * multi line text
  * 예외처리 : exception 이 나도 프로그램이 죽지 않게 하자. component 나 gameObject 에서 fake null 처리
  * opaque 와 alpha blend 분리. 현재는 모두 alpha blend 로 처리중
  * texture 나 material 을 해제했다가 다시 할당받는 경우가 꽤 있을 수 있으므로, 
    reference 가 0 이 되자마자 삭제하지 말고, 일정 시간 동안 유예를 줘야한다
  * UI 조작시 consume 된 조작은 거기에서 stop
  * (최적화) Update 가 필요한 component 만 update 하도록


